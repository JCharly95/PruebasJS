function numRandom(numero){
    return (Math.random()*numero).toFixed(0);
}

function randColorRGB(){
    let color = "("+numRandom(255)+","+numRandom(255)+","+numRandom(255)+")";
    return "rgb" + color;
}

function valorRandom(cant){
    let valores = [];

    
}